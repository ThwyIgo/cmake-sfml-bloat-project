#pragma once
// This file was generated by CMake

#include <boost/dll/runtime_symbol_info.hpp>
#include <filesystem>

#define PROJECT_VERSION "@PROJECT_VERSION@"
#define SYSTEM_NAME "@CMAKE_SYSTEM_NAME@"

/* DATA_PATH is relative to install location. PWD isn't necessarily the install location when the program is
 * launched. */
const std::filesystem::path DATA_PATH = (boost::dll::program_location().parent_path() / "@DATA_PATH@").generic_string();
